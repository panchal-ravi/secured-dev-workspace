# Pinned to the versions verified live against the deepagents API (2026-07-06).
# deepagents pulls langchain-core / langchain / langgraph transitively; langgraph
# is pinned too because main.py imports InMemorySaver from it directly.
deepagents==0.6.12
langchain-mcp-adapters==0.3.0
langchain-openai==1.3.3
langgraph==1.2.7
fastapi==0.139.0
uvicorn==0.50.1
pyyaml==6.0.3
